AI Chatter Executor 3.5.9

- Stores activeUrl/activeTitle for provider sessions.
- Returns to sticky activeUrl before next job instead of provider home.
- Uses extension strict send confirmation to avoid treating stuck input as reply.
- Keeps managed provider sessions and memory rollover from R3.5.8.
